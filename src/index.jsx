import ReactDom from 'react-dom/client'
import { StrictMode } from 'react'

const root = ReactDom.createRoot(document.getElementById('root'))

root.render(
  <StrictMode>
    <h1>Reausable Components</h1>
  </StrictMode>
)
